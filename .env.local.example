# Optional, non-secret local development overrides.
# Copy to .env.local only when a default port or timeout must be changed.
# Supabase URLs and keys are discovered from `supabase status` and must not be placed here.

API_PORT=5080
WEB_PORT=3000
WORKER_POLL_SECONDS=2
HEALTHCHECKS_TIMEOUT_SECONDS=3
