# Expected results

Expected semantic properties are stored in `../manifest.json` so the small
corpus has one machine-readable source of truth. S2-T01/S2-T02 tests additionally
use the text-free `ParsedDocumentCanonicalProjection` to prove repeat and
parallel determinism for parsed structure, effective formatting, and provenance
without persisting paragraph content in snapshots.
