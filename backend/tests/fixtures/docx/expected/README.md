# Expected results

Initial expected semantic properties are stored in `../manifest.json` so the
small corpus has one machine-readable source of truth. Add a focused expected
parser or fixer result here only when a stable contract needs more detail.
